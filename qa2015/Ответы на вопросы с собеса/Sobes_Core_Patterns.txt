Какие темы подучить:

1) Junit тестирование зачем и как проводится.
2) Зачем MOCK объекты, когда их использовать, а когда нет.
3) Какие шаблоны проектирования вы знаете? Тут скорее именно для j2EE, а не дизайн-патерны. Какие проблемы решает MVC. Чем он отличается от MVP.
4) Какие паттерны я использовал и почему.
5) Перечислить и объяснить SOLID паттерны.
6) Алгоритмы сортировок: пузырьком и др, какие я знаю, плюсы и минусы. Как и реализовать на java.
7) Concurrency, как это реализованов в 5-й джаве. 
8) Если есть много миллионов файлов как распараллелить их обратку на 4-х ядерном процессоре.
9) Сколько элементов будет создано при инициализации HashMap?
10) Зачем вообще hashmap и где используются, для каких целей?
11) Дефолтный методы в интерфейсах, когда появилось и зачем? КАк выходить из ситуации с множественным наследованием в Джава 8.

Практические задачи:

1) По мок-объектам - как проинициализировать класс, где статик поле при инициализации выдаёт эксепшн. Статик поле инициализируется методом, метод throws exception, а нужно проверить другой метод summ
2) Два null в CompareTo, баг проги, но что будет? - вечный цикл, т.к. на вход сначала придут с1 и с2, а потом с2 и с1.
3) Что выполнит java при перехвате exception-а:
try{}
catch {return 1;}
finally {return 2;}
4) Написать код программы для выбора из массива int чисел подмножества с наибольшей суммой.
5) Когда есть например класс студент учитель, каждый студент знает когото, учителя знают все, учитель не знает никого. КАк лучше найти учителя среди всех людей?
